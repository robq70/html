function addnumbers(x, y) {
	return x + y
}
// addnumbers(5, 9)
// funkcje w konsoli wywołujemy nazwafunkcji(nazwy argumentów np. cyfry)

function addnumbers(x, y) {
	console.log(x + y)
}
addnumbers(5, 8)
// tutaj funkcja wywyołuje się sama

function test() {
	console.log('Cześć !')
}
// wywołanie funkcji bez argumentów test
test()
