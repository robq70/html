// zmienne zapisujemy po angielsku
const name = 'Lisa'

//Nie wolno w nazwach używać nazw zastrzeżonych dla poleceń, polskich nazw, cyfry na początku, myślnika.

const favcolor = 'blue'

// camelCase - notacja wielbłądzia
const favColor = 'blue'

const 1test = 1234 // nie używamy cyfr na początku nazwy zmiennej
const fav-color = 'blue' // nie używamy znaku minusa
const fav_color = 'blue' // tak można ale lepiej camelCase

const User = 'abcd' // duża litera na początku to zapis zarezerwowany dla klas

const USER = 'abcd' // duże litera to zapis zarezerwowany dla ważnych zmiennych w które nie wolno ingerować

console.log(User, USER)

const API_KEY = 'igff9887f' // klucz do uwierzytelniania aby skorzystać z zakupionego API
